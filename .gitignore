# Chamber runtime state — never track
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite.bak
data/*.sqlite*
pilot.jsonl
*.jsonl

# Checkpoint receipts / exports (regenerable). Both spellings: the default
# basename is `checkpoint.json`, which `checkpoint-*.json` does not match.
checkpoint.json
checkpoint-*.json
anchors.jsonl

# Secrets & credentials
.env
.env.*
*.pem
*.key
!models/**/*.key

# Dependencies / build
node_modules/
dist/
build/

# Sandbox scratch
chamber-sbx-*/

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/
.claude/

# Logs
*.log
logs/

# probe scratch
probes/seatbelt/work/inside.txt
probes/seatbelt/work/*.txt
.superpowers/

# vigil run records (local, content-free; see engines/telemetry.md)
.vigil/
dist

# Scratch copies of the harness written by probes/harness_declaration_guards.ts.
# Removed in its finally block; ignored here in case it is killed mid-run.
tests/.harness-guard-probe-*.ts
